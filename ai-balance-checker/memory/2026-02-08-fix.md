# 2026-02-08 记录（更新）

## 问题修复：定时汇报

### 用户反馈
用户投诉：很久没有工作，说半小时汇报一次也没有执行

### 原因分析
检查 cron 任务发现：
- 原有任务只有：每天 8:00 UTC（凌晨 4 点）+ 每周日 9:00 UTC
- 没有"每 30 分钟"的定时任务
- `wakeMode` 设置为 `next-heartbeat`，只在心跳时触发

### 解决方案
✅ 创建新的 cron 任务："进度主动汇报"
- 执行频率：每 30 分钟（1800000 毫秒）
- Wake 模式：`now`（立即触发，不依赖心跳）
- 内容：定时进度汇报，包含：
  - 当前时间戳
  - QR 工具箱状态
  - AI 余额查询工具状态
  - 待办事项列表

### 任务配置
```json
{
  "name": "进度主动汇报",
  "schedule": {
    "kind": "every",
    "everyMs": 1800000
  },
  "wakeMode": "now",
  "payload": {
    "kind": "systemEvent",
    "text": "📊 定时进度汇报..."
  },
  "enabled": true
}
```

### 状态
✅ 任务已创建并启用
✅ 下次执行：立即触发（1770571775036 ms）

---

## 问题修复：语言切换

### 用户反馈
"中英文切换不了。i18多语言界面也没有实现"

### 原因分析
检查代码发现：
1. ❌ app.mjs 中所有动态生成的 HTML（innerHTML）都是硬编码的中文
2. ❌ 例如：`"URL / 文本"`、`"电话号码"`、`"请输入内容以生成二维码"` 等
3. ❌ 这些硬编码文本不会因为 HTML data-i18n 属性的更新而改变
4. ❌ i18n.js 只更新静态 HTML 元素，不处理动态生成的内容

### 解决方案

#### 1. 添加 getI18n() 辅助函数
```javascript
function getI18n(key) {
  if (window.i18n && window.i18n.getTranslation) {
    return window.i18n.getTranslation(key, 'en');
  }
  // Fallback English translations
  const fallbacks = {
    'wifi.security': 'Security',
    'wifi.wpa': 'WPA/WPA2',
    // ... 50+ 翻译 keys
  };
  return fallbacks[key] || key;
}
```

#### 2. 批量替换硬编码中文
使用 sed 批量替换 app.mjs 中所有硬编码的中文：
- `"URL / 文本"` → `getI18n("text.label")`
- `"电话号码"` → `getI18n("tel.label")`
- `"请输入内容以生成二维码"` → `getI18n("generate.enterContent")`
- `"生成失败"` → `getI18n("generate.failed")`
- ... 总共 50+ 处替换

#### 3. 添加完整的 i18n 翻译
在 i18n.js 中添加所有需要的翻译 keys：
- WiFi 相关：security, ssid, password, hidden, wpa, wep, nopass
- Text 相关：label, placeholder
- Email 相关：to, toPlaceholder, subject, body
- SMS 相关：to, body
- Geo 相关：lat, lng, latPlaceholder, lngPlaceholder, query, queryPlaceholder
- vCard 相关：name, org, title, tel, email, url, adr, note
- 通用：enterContent, failed, noContent

#### 4. 添加语言变化事件监听
```javascript
// 在 setLanguage 中触发事件
function setLanguage(lang) {
  // ... 更新页面 ...

  // 触发自定义事件
  const event = new CustomEvent('i18n-changed', { detail: { lang } });
  window.dispatchEvent(event);
}

// 在 app.mjs 中监听事件
window.addEventListener('i18n-changed', () => {
  // 重新渲染当前 tab 的动态内容
  render(); // 或 buildBatchPreview()
});
```

### 状态
✅ app.mjs 所有硬编码中文已替换为 getI18n() 调用
✅ i18n.js 添加了 50+ 个新翻译 keys
✅ 添加了 'i18n-changed' 自定义事件
✅ app.mjs 监听事件并重新渲染
✅ 代码已提交并推送到 GitHub

### 修改时间
2026-02-08 17:30 UTC

---
